export interface PlaybackData {
    rate: number;
    playing: boolean;
    duration: string,
    durationSeconds: number,
    playtimeSeconds: number,
    playtimeCap: number,
    playtime: string,
    volume: number,
    paused: boolean,
    loading: boolean
}